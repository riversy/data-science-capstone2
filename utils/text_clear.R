source('utils/requirements.R')


clean_text_data <- function(text_corpus) {
  
  # 1. Split to sentences
  
  
  
  # 2. Text to lower
  
  # 3. Extract words only
  
  
  # 4. Deal with 'you' 'll' (you will) 

  # 5. Remove profanity words
  
  # 6. Concat it back to corpus again
  
  text_corpus

}